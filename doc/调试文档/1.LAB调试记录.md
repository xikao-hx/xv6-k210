
### COW问题分析

**遗留问题总结**：

* 这个实验的竞态问题，不知道为什么看似简单，但是还有有点复杂的；总体上来看，我觉得这个写的代码，没有很好的处理页面的用户页表数的“引用计数”的竞态问题

#### 问题1：remap，mappages传递的参数va是页对齐，才能映射size大小，否则是size+1

```c
/*
问题查找过程：
	1) 通过调用栈，发现就是cowalloc里面调用mappages出错
	2) 通过mappages中增加打印，能发现panic时，PTE的值不对
	3) 后续进一步增加打印可以发现，多访问了一个页面
*/

$ panic: remap
backtrace:
0x000000008000058c
0x0000000080001324
0x0000000080001874
0x0000000080002b5c
xikao@xikao-virtual-machine:~/xv6/xv6-labs-2020$ addr2line -e kernel/kernel
/home/xikao/xv6/xv6-labs-2020/kernel/printf.c:138
/home/xikao/xv6/xv6-labs-2020/kernel/vm.c:160
/home/xikao/xv6/xv6-labs-2020/kernel/vm.c:372

// 总结这个函数的va，size必须是页对齐的，否则访问的就不是本要访问的页面了，会访问到别的页面
int mappages(pagetable_t pagetable, uint64 va, uint64 size, uint64 pa, int perm)

$ a1: 0x0000000000001000
va1: 0x00000000000014a8    // 传入的参数没有页对齐
pte1: 0x0000000021fd81da 
a2: 0x0000000000002000     // 多访问了一个页面
va2: 0x00000000000014a8
pte2: 0x0000000021fd7d0b
panic: remap               // 导致了remap的出现
backtrace:
0x000000008000058c
0x00000000800013e0
0x000000008000195c
0x0000000080002c6c

// 正常应该只访问一个页面
a1: 0x0000000000001000
va1: 0x0000000000001000
pte1: 0x0000000021fd81da

// 结论：mappages的参数必须页对齐
```

#### 问题2：内存泄漏，竞态导致cnt>1时进入，而退出的时候cnt=0，所以必须free掉

```c
/*
问题查找过程：
	1) 对usertests进行注释，发现sharedfd这个测试，会存在内存泄漏
	2) 对addmapcnt和reducemapcnt增加打印，确认物理地址对应的映射的情况
*/

// 出现内存泄漏
FAILED -- lost some free pages 32337 (out of 32344)  

test sharedfd: 
pa1: 0x0000000087f47000
cnt1: 2
pid: 3
...
pa1: 0x0000000087f4e000
cnt1: 2
pid: 3    // fork: usertests --> shared
    
    
pa2: 0x0000000087f4e000
cnt2: 1
pid2: 5
ppid2: 3   // 写
    
pa1: 0x0000000087f47000  // fork: shared --> child 
cnt1: 3
pid: 5
...
pa1: 0x0000000087f36000
cnt1: 3
pid: 5

/* 问题所在 */
// 父子进程同时操作了同一个文件，引发后面的内存泄漏的出现
// 这个地址不是很明白为什么不是上面的地址?
pa1: 0x0000000080518000
cnt1: 2
pid: 5

// 子进程操作
pa2: 0x0000000080518000
cnt2: 1
pid2: 6 
ppid2: 5

// 父进程操作
pa2: 0x0000000080518000
cnt2: 0      // 应该要释放了，但是没有导致后面出现内存泄漏，这里不释放的话，没有进程来释放了，因为PTE的值已经被修改了
pid2: 5
ppid2: 3
OK

```

#### 问题3：reparent出错：竞态问题？

```c
void *
kalloc(void)
{
  struct run *r;

  acquire(&kmem.lock);
  r = kmem.freelist;
  if(r) {
    kmem.freelist = r->next;
    acquire(&kmapcnt.lock);      // 这里必须写在里面
    kmapcnt.quota[(uint64)r / PGSIZE] = 1; 
    release(&kmapcnt.lock);
  }
  release(&kmem.lock);

  if(r)
    memset((char*)r, 5, PGSIZE); // fill with junk
  return (void*)r;
}
```


### mmap问题

#### **局部变量和指针变量**

```c
int mmap_handler(uint64 va, uint64 scause)
{
  struct proc* p = myproc();
  pagetable_t pagetable = p->pagetable;
  int i = 0;
  struct vma_areas* vma;  // 这个地方必须使用指针，或者说从可行上来讲使用指针较好，因为如果修改值的话，修改的是局部变量的值，不是数组中的值了

  // 根据地址查找属于哪一个vma
  for (; i < NVMA; i ++) {
    vma = &p->vma[i];
    if (vma->used && va >= vma->addr && va <= (vma->addr + vma->length - 1)) {
      break;
    }
  }
}
```

#### **脏内存**

```c
int mmap_handler(uint64 va, uint64 scause)
{
  ...
  void* pa = kalloc();
  if (pa == 0) {
    return -1;
  }
  memset(pa, 0, PGSIZE);   // 这个地方必须memset否则测试的时候出错，这里会检查内容为空
    
/*
$ mmaptest
mmap_test starting
test mmap f
mismatch at 6144, wanted zero, got 0x5
mmaptest: mmap_test failed: v1 mismatch (2), pid=6
$ 
*/
```

#### **页面访问出现错误**

```c
int
fork(void)
{
    ...
        
    for (i = 0; i < NVMA; i ++) {
    struct vma_areas *vma = &p->vma[i];
    if (vma->used) {
      memmove(&np->vma[i], vma, sizeof(struct vma_areas));  // vma不用再&了
      filedup(vma->vfile);
    }
  }

  safestrcpy(np->name, p->name, sizeof(p->name));
  ...
}

/*
fork_test starting
scause 0x000000000000000d
sepc=0x000000008000459c stval=0x0000000000000024
panic: kerneltrap 
*/
```

#### 关于页面对齐的一个问题

```c
uint64 sys_mmap(void)
{
  ...

  for (int i = 0; i < NVMA; i ++) {
    if (p->vma[i].used == 0) {
      p->vma[i].addr = p->sz;   // 这个地方不是页面对齐的
      ...

      return p->vma[i].addr;
    }
  }

  return err;
}

int mmap_handler(uint64 va, uint64 scause)
{
  ...

  void* pa = kalloc();
  if (pa == 0) {
    return -1;
  }
  memset(pa, 0, PGSIZE);  

  ilock(vma_file->ip);
  int offset = vma->offset + PGROUNDDOWN(va - vma->addr);
  int readbytes = readi(vma_file->ip, 0, (uint64)pa, offset, PGSIZE);   // 这里却把直接文件读入一个页面，然后用虚拟地址去访问，这里有一个假设就是p->sz最开始的时候是页面对齐的
  if (readbytes == 0) {
    iunlock(vma_file->ip);
    kfree(pa);
    return -1;
  }
  iunlock(vma_file->ip);
    
  ...
}

// 通过以下测试证明了上述内容
int
main(int argc, char *argv[])
{
  sbrk(100);  
  /*
  通过sbrk使得p->sz增大了，出现了页面不对齐，并分配了一个页面，正常测试，应该是这里触发页面错误，我们写的代码分配一个页面，然后读入一个页面的文件内容，这样用虚拟地址访问就对上了
  */
  mmap_test();
  fork_test();
  printf("mmaptest: all tests succeeded\n");
  exit(0);
}

/*
$ mmaptest
mmap_test starting
test mmap f
mismatch at 0, wanted 'A', got 0x0   // 这里得到0x0就是因为多分配了一个页面出来，所以读到0值，严重可能还是出现panic，出现remap
mmaptest: mmap_test failed: v1 mismatch (1), pid=6
$ 
*/
```


###  LAB3遇到的问题

```shell
scause 0x000000000000000d
sepc=0x0000000080001208 stval=0x00000000000007f8
panic: kerneltrap
    
# 调用栈
/home/xikao/xv6/xv6-labs-2020/kernel/printf.c:164
/home/xikao/xv6/xv6-labs-2020/kernel/trap.c:153 (discriminator 1)
kernelvec.o:?
/home/xikao/xv6/xv6-labs-2020/kernel/vm.c:195
/home/xikao/xv6/xv6-labs-2020/kernel/vm.c:156
/home/xikao/xv6/xv6-labs-2020/kernel/proc.c:130  # 这个地方一直调用下去导致的panic
/home/xikao/xv6/xv6-labs-2020/kernel/proc.c:254
/home/xikao/xv6/xv6-labs-2020/kernel/main.c:32
/home/xikao/xv6/xv6-labs-2020/kernel/riscv.h:6

# p->pagetable = ukvminit(); pagetable不对，应该是p->kernel_pagetable = ukvminit();
# ukvmmap(p->k_pagetable, va, (uint64)pa, PGSIZE, PTE_R | PTE_W); 由于没有页表内容进一步导致这里出现页面异常，所以调用了trap函数
```

```shell
panic: uvmunmap: walk
backtrace:

# 调用栈
/home/xikao/xv6/xv6-labs-2020/kernel/vm.c:224
/home/xikao/xv6/xv6-labs-2020/kernel/vm.c:242  # 发生panic前一个调用的函数
/home/xikao/xv6/xv6-labs-2020/kernel/proc.c:232
/home/xikao/xv6/xv6-labs-2020/kernel/proc.c:155
/home/xikao/xv6/xv6-labs-2020/kernel/proc.c:469
/home/xikao/xv6/xv6-labs-2020/kernel/sysproc.c:40
/home/xikao/xv6/xv6-labs-2020/kernel/syscall.c:319

# uvmunmap(pagetable, PLIC, 0x400000, 0);    调用的这个函数，大小不对，应该是0x400000 / PGSIZE，导致到访问了页表，发生了panic
```

```c
// 编译了太多用户程序，测试usertests，出现如下错误，取消掉一部分就好了
// test writetest: OK
// test writebig: panic: balloc: out of blocks
```

```c
// panic: freewalk: leaf

void
proc_freekpagetable(pagetable_t pagetable, uint64 kstack, uint64 sz)
{
  ukvmunmap(pagetable);
  uvmunmap(pagetable, 0, PGROUNDUP(sz) / PGSIZE, 0);   // 忘记munmap
  uvmunmap(pagetable, kstack, 1, 1);
  uvmfree(pagetable, 0);
}

upg2ukpg(p->pagetable, p->kpagetable, 0, sz);

// panic: upg2ukpg: walk err1!
upg2ukpg(p->pagetable, p->kpagetable, 0, sz);  // exec中放的太前了，p->pagetable还是旧的值，导致访问这个页面出错

// test execout: scause 0x000000000000000d
// sepc=0x0000000080000ca0 stval=0x050505006f686365
// panic: kerneltrap

ukvmdealloc(p->kpagetable, addr, addr + n, 0);  // brk中减少sz时，不能释放内存，应该是通过进程页表释放，这里是去除映射

/*
错误分析：
scause 0x000000000000000d：
    在 RISC-V 中，scause 寄存器保存了异常/中断的原因
    0x000000000000000d = 13 (十进制)
    根据 RISC-V 特权架构规范：
    12: Instruction page fault (指令页面错误)
    13: Load page fault (加载页面错误)
    15: Store/AMO page fault (存储页面错误)
    所以这是一个 加载页面错误。
sepc=0x0000000080000ca0：
    这是发生错误时的程序计数器值
    地址 0x80000ca0 在内核地址空间（KERNBASE = 0x80000000）
    说明错误发生在内核代码中
stval=0x050505006f686365：
    这是触发页面错误的虚拟地址
    注意：这个地址看起来很奇怪，像是 ASCII 字符：
    0x6f686365 = "ohce" (little-endian)
    可能是一个字符串指针或错误的数据
panic: kerneltrap：
	内核捕获了无法处理的陷阱，触发 panic

与缺页中断的区别：
1. 缺页中断 (Page Fault)：
    发生场景：通常发生在用户空间
    处理方式：xv6 的 usertrap() 处理
    原因：合法的页面错误（如懒分配、写时复制、栈增长等）
    结果：通常可以修复并继续执行

2. 内核陷阱 (kerneltrap)：
    发生场景：发生在内核空间
    处理方式：xv6 的 kerneltrap() 处理
    原因：内核代码访问了无效地址
    结果：通常是严重错误，导致 panic
*/
```

**总结**：

* 分析panic
  * 分析调用栈，最主要就是找到出错的源头在哪里，然后进行分析
* 分析页面错误问题
  * 先

3.5 lab5补充==（完成）==

### LAB5遇到的问题

**假问题**：

```c
int
uvmlazymalloc(pagetable_t pagetable, uint64 va)
{
  struct proc *p = myproc();
  char *mem;

  pte_t *pte = walk(pagetable, va, 0);
  if(pte != 0 && (*pte & PTE_V)) {
    return 0;   // 已经映射，直接返回成功
  }

  va = PGROUNDDOWN(va);
  mem = kalloc();
  if(mem == 0){
    // printf("uvmlazymalloc: kalloc fail\n");   
    return -1;
  }
  ...
}

// 增加的打印导致个人误判为失败，但是这个测试用例就是会耗尽内存，导致一直一个不存在的问题，其实已经可以通过了
```

**假remap**:

```c
// usertests mem
// 这个用例会申请内存，有释放，有申请，用户态是通过brk来分配，测试中的 free 只是标记内存可用，不会立即调用 sbrk 来缩小进程地址空间。也就是不会把内存进行清除(脏内存)，导致误判为remap，这也就是为什么用memmove，而不是用mappages的原因所在

upg2ukpg(p->pagetable, p->kpagetable, va, va + PGSIZE);
// if(mappages(p->kpagetable, va, PGSIZE, (uint64)mem, PTE_W|PTE_X|PTE_R) != 0){
//   uvmunmap(pagetable, va, PGSIZE / PGSIZE, 1);
//   printf("uvmalloc: mappages fail\n");
//   kfree(mem);
//   return -1;
// }
```

**进程地址空间收到限制：**==后面要完成如何扩大进程地址空间==

```c

uint64
sys_sbrk(void)
{
  int addr;
  int n;
  struct proc *p = myproc();
  int sz = p->sz;

  if(argint(0, &n) < 0)
    return -1;
  addr = p->sz;

  // 注释掉这个可以通过测试，但是REGION_SZ不能太大
  if ((addr + n) >= PLIC) {
    return -1;   // 这里限制了
  }
  ...
}
/*
lazytests的测试sz大小需要修改下
    #define REGION_SZ (1024 * 1024 * 1024)
    PLIC  -->  201326592  -->  由于进程内核页表的限制，导致不能进程的地址空间不能太大，否则会覆盖内核地址空间，导致出现错误
*/ 

```


### LAB6遇到的问题

**无法正常启动**

```c
if (va < p->sz) {
      if (uvmcowpage(pagetable, va) == 0) {
        if (uvmcowmalloc(pagetable, PGROUNDDOWN(va)) == 0) {  // 用了uvmlazymalloc函数，且返回值判断不对
          p->killed = 1;
        }
      } else if (PGROUNDUP(p->trapframe->sp) - 1 < va) {
        if (uvmlazymalloc(pagetable, PGROUNDDOWN(va)) != 0) {
          // printf("usertrap(): uvmlazymalloc\n");
          p->killed = 1;
        }
      }
    } else {
      printf("usertrap(): addr is illegal\n");
      p->killed = 1;
    }
```

**进程内核页表：**导致命令无法执行，可以正常启动敲回车

```c
void *  
uvmcowmalloc(pagetable_t pagetable, uint64 va) 
{
    // 由于对进程页表进行过修改，所以必须要，同步修改进程的内核页表
    // upg2ukpg(p->pagetable, p->kpagetable, va, va + PGSIZE);
}
```

**总结**：

* 优化了一个kalloc、kfree中锁的使用，但是整个过程还不是很清楚
  * 主要是使用一个把锁还是两把锁
  * 锁的使用顺序应该保持一致

* 各种分配主要是加上page table实验后，==不要忘记同步修改进程内核页表==

### LAB10遇到的问题

**页面映射错误**：==没想到最后来了个大的，一失足成千古恨==

```c
/*
pagetable: 0x0000000087eb8000, va: 0x0000000000004000, pa: 0x0000000087e67000   // 前后对应的物理地址不对
mismatch at 0, wanted 'A', got 0x97
mmaptest: mmap_test failed: v1 mismatch (1), pid=6
pagetable: 0x0000000087eb8000, va: 0x0000000000004000, pa: 0x0000000000001000

pagetable: 0x0000000087eb8000, va: 0x0000000000004000, pa: 0x0000000087e67000   // 前后对应的物理地址不对
pagetable: 0x0000000087eb8000, va: 0x0000000000004000, pa: 0x0000000087e67000
mismatch at 0, wanted 'A', got 0x97
mmaptest: mmap_test failed: v1 mismatch (1), pid=6
pagetable: 0x0000000087eb8000, va: 0x0000000000004000, pa: 0x0000000000001000
*/

printf("pagetable: %p, va: %p, pa: %p\n", pagetable, va, pa);
if (mappages(pagetable, PGROUNDDOWN(va), (uint64)pa, PGSIZE, pte_flags) != 0) {  // 这里参数传递错了，导致虚拟地址和物理地址一致对应不上
kfree(pa);
return -1;
}

// 这才是对的
if (mappages(pagetable, PGROUNDDOWN(va), PGSIZE, (uint64)pa, pte_flags) != 0) {
    kfree(pa);
    return -1;
}

// 这里还有一个问题是mmap会导致p->sz变大，但是munmap会修改页表，释放对应的内存，所以这会导致进程sz范围大小的虚拟地址空间，存在空洞问题，因为munmap修改页表后并不修改p->sz了，但是也不影响后面进程的退出和物理地址的释放，因为munmap页表修改了，物理地址也被释放了。也没有办法修改p->sz，所以一般linux中一般会有专门的页面映射区域
```

**总结**：

* 这个mmap的实现是**丐版的**，但是可以清楚的知道大概的流程
  * 只是实现了文件映射功能
  * 只能处理sz是对齐的情况