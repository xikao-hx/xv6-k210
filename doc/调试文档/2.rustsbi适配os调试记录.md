# 任务

任务1：完成os启动，功能正常。/home/xikao/xv6-k210-opensbi/xv6
* rustsbi引导启动xv6在k210平台上成功启动，怀疑是中断的问题
    * 现在能启动第一个进程并出现shell表示，但是之后hang住，且需要把dma的驱动改为轮询模式，若是中断模式，则无法启动到shell，会在sdcard_read_sector过程中卡住
* 参考/home/xikao/xv6-k210-opensbi/xv6-k210，参考这个文件夹下的内容，这个可以在k210上成功运行
* trap中加调试，打印如下
```shell
$ scause: 5
scause: 1
DEBUG: Received external IRQ: 27   # dma中断
dmac_intr: 1342177280
dmac_intr finish
scause: 5
scause: 5
scause: 5
scause: 5
```

# 任务1：K210 平台 rustsbi 启动 xv6 — 调试与修复记录

## 项目背景

在 K210 硬件平台上使用 rustsbi 作为 bootloader 启动 xv6 操作系统。K210 使用 RISC-V 特权级规范 v1.9.1（与常见的 v1.10+ 有多处不兼容）。

参考成功实现：`/home/xikao/xv6-k210-opensbi/xv6-k210/`（使用 OpenSBI）。

## 初始状态

- 系统能启动到 shell 提示符 `$`，但敲击回车后 hang 住
- DMA 驱动必须使用轮询模式（中断模式会导致 SD 卡读取卡死）
- 参考 xv6-k210 可以成功运行

## 问题一：键盘输入无响应（S 态外部中断不工作）

### 现象

系统启动到 shell 后，敲击键盘回车，系统 hang 住，没有任何响应。

### 调试过程

1. **定位中断路径**：K210 v1.9.1 没有 S 模式外部中断（SEIE），rustsbi 通过"软代理"方式转发外部中断：
   - M 模式收到 MachineExternal 中断
   - 设置 `stval = 9`，触发 S 模式软件中断（`mip::set_ssoft()`）
   - 清除 `mie.mext`，等待 S 模式处理完后再开启

2. **检查 SBI 扩展 ID**：内核通过自定义 SBI 扩展 `sbi_set_extern_interrupt()`（注册中断处理函数）和 `sbi_set_mie()`（重新使能 M 模式外部中断）与 rustsbi 交互。

3. **发现根因**：`sbi.h` 中的 SBI 扩展 ID 与 rustsbi 中注册的不匹配：
   - 内核使用：`0x09000004` / `0x09000005`
   - rustsbi 期望：`0x0A000004` / `0x0A000005`
   - 导致第一次中断处理后 `mext` 被清除但无法重新使能，后续中断全部丢失

### 修复

**文件：`xv6/kernel/include/sbi.h`**

将 SBI 扩展 ID 从 `0x09000004`/`0x09000005` 修改为 `0x0A000004`/`0x0A000005`：

```c
static inline void sbi_set_extern_interrupt(unsigned long func_pointer) {
    asm volatile("mv a6, %0" : : "r" (0x210));
    SBI_CALL_1(0x0A000004, func_pointer);  // 原为 0x09000004
}

static inline void sbi_set_mie(void) {
    SBI_CALL_0(0x0A000005);  // 原为 0x09000005
}
```

**文件：`xv6/bootloader/rustsbi-k210/src/main.rs`**

三处修改：

```rust
// 修改1：初始化时开启 mext 和 msoft（约第231-234行）
mie::set_mext();   // 新增：使能 M 态外部中断
mie::set_msoft();  // 保留

// 修改2：0x0A000005 处理函数正确设置 mext（约第380-383行）
} else if trap_frame.a7 == 0x0A000005 {
    unsafe {
        mie::set_mext();  // 正确的扩展 ID
    }
    trap_frame.a0 = 0;
    trap_frame.a1 = 0;
}

// 修改3：MachineExternal 中断处理（约第436-441行）
Trap::Interrupt(Interrupt::MachineExternal) => {
    unsafe {
        llvm_asm!("csrw stval, $0" :: "r"(9) :: "volatile");
        mip::set_ssoft();
        mie::clear_mext();  // 转发到 S 态后关闭 mext
    }
}
```

### 结果

修复后键盘输入可以工作，回车后系统能响应。

---

## 问题二：进程被 `scause 5/7` 异常杀死

### 现象

从键盘输入命令后，系统输出：

```
usertrap(): unexpected scause 0x0000000000000007 pid=...
panic: init exiting
```

### 调试过程

1. **分析 scause 编码**：RISC-V v1.10+ 中 store page fault 为 `scause=15`，load page fault 为 `scause=13`。但 K210 v1.9.1 使用统一的访问异常编码：load=5，store=7。

2. **检查 trap.c**：内核的 `usertrap()` 只检查 `scause == 13 || scause == 15`（v1.10+ 编码），K210 上的 `scause=7` 未被识别，进程被直接杀死。

3. **影响范围**：COW（Copy-On-Write）、mmap、lazy allocation 的缺页异常处理全部无法触发。

### 修复

**文件：`xv6/kernel/trap/trap.c`（第79-83行）**

在 K210 平台添加 v1.9.1 的异常编码：

```c
} else if (r_scause() == 13 || r_scause() == 15
#ifndef QEMU
             || r_scause() == 5 || r_scause() == 7
#endif
) {
```

### 结果

进程的缺页异常（包括 COW 写保护异常）能够正确触发处理函数。

---

## 问题三：非法指令崩溃（TLB 不一致导致指令读取错误）

### 现象

系统启动后出现非法指令崩溃：

第一次（修复前）：
```
[rustsbi] panicked at 'invalid instruction! mepc: 0x1280, instruction: 6a0500d7'
```

第二次（添加部分 sfence_vma 后）：
```
$ [rustsbi] panicked at 'invalid instruction! mepc: 00000000000011c2, instruction: 00000797'
```

地址 `0x11c2` 是 shell 的 `free` 函数中的 `auipc a5, 0x0` 指令——这是一条**合法指令**。

### 调试过程

1. **分析矛盾**：`0x00000797` = `auipc a5, 0` 是合法的 RV64 指令，CPU 不应在此触发非法指令异常。

2. **关键发现**：rustsbi 在 M 模式读取 `mepc` 处的指令时使用物理地址（M 模式 Bare 寻址），不是通过 S 模式页表翻译。CPU 实际执行的指令是通过 TLB 缓存的虚拟地址翻译获取的——TLB 中可能是**过期映射**，指向已被释放并重新分配的物理页。

3. **定位根因——TLB 不一致**：K210 v1.9.1 使用 `sfence.vm`（而非 v1.10+ 的 `sfence.vma`）刷新 TLB。COW 机制在多处修改 PTE 后**未调用 sfence_vma**，导致：
   - `uvmcowmalloc()` 释放旧物理页后，TLB 仍缓存 VA→旧PA 的映射
   - CPU 执行该 VA 处代码时，取到已被 `kfree` 填充为垃圾数据（0x01）的物理页内容
   - 垃圾数据被 CPU 解码为非法指令 → M 模式异常 → rustsbi panic

4. **深入分析 kpagetable 同步问题**：
   - xv6 在 K210 上通过 kpagetable 访问用户内存（`copyin_new`/`copyinstr_new` 直接使用虚拟地址）
   - `fork()` 中 `uvmcopy` 修改了父进程**用户页表**的 PTE（添加 COW 标记），但**未同步更新父进程的 kpagetable**
   - `fork()` 调用 `upg2ukpg` 更新子进程的 kpagetable，但遗漏了父进程
   - `growproc()` 增长内存后**完全没有**调用 `upg2ukpg`
   - 导致 kpagetable 中的 PTE 与用户页表不一致，TLB 刷新后仍加载过期映射

5. **指令缓存问题**：K210 v1.9.1 的 `sfence.vm` 可能只刷新数据 TLB，不刷新指令 TLB。需要在 `sfence_vma` 后追加 `fence.i` 指令确保指令缓存一致性。

### 修复

#### 修改 1：`xv6/kernel/include/riscv.h` — sfence_vma 添加 fence.i

```c
static inline void
sfence_vma()
{
  asm volatile("sfence.vma zero, zero");
#ifdef QEMU
  // QEMU handles TLB coherency automatically
#else
  // K210 v1.9.1: sfence.vm may not flush the instruction TLB,
  // so we also issue fence.i to flush the instruction cache.
  asm volatile("fence.i");
#endif
}
```

说明：`sfence.vma zero, zero` 在 K210 上是非法指令，会陷入 M 模式由 rustsbi 仿真为 `sfence.vm`。rustsbi 同时设置 `mstatus.VM=9`（启用 Sv39 分页）。追加的 `fence.i` 确保指令缓存也被刷新。

#### 修改 2：`xv6/kernel/vm/vm.c` — uvmcowmalloc 两个分支添加 sfence_vma

**单引用路径**（仅当前进程引用该页，直接标记可写）：
```c
if (kgetquota((void *)pa) == 1) {
    *pte |= PTE_W;
    *pte &= ~PTE_COW;
    upg2ukpg(p->pagetable, p->kpagetable, va, va + PGSIZE);
    sfence_vma();  // 新增：刷新 TLB
    return (void *)pa;
}
```

**多引用路径**（多进程共享，分配新页并释放旧页）：
```c
    upg2ukpg(p->pagetable, p->kpagetable, va, va + PGSIZE);
    kfree((void *)pa);       // 旧页可能被填充为垃圾数据
    sfence_vma();            // 新增：必须刷新 TLB，否则 CPU 可能读到垃圾
    return (void *)mem;
```

这是**最关键的修复**——`kfree` 将 refcount 降为 0 的页面用 `0x01` 填充，若 TLB 仍有该页的缓存映射，CPU 执行该地址时会取到非法指令。

#### 修改 3：`xv6/kernel/vm/vm.c` — uvmcopy 末尾添加 sfence_vma

```c
    kaddquota((void *)pa);
  }
  sfence_vma();  // 新增：COW 标记修改父进程 PTE 后刷新 TLB
  return 0;
```

#### 修改 4：`xv6/kernel/proc/proc.c` — fork 同步父进程 kpagetable

```c
  upg2ukpg(np->pagetable, np->kpagetable, 0, np->sz);
  sfence_vma();            // 新增：子进程 kpagetable 更新后刷新 TLB

  np->state = RUNNABLE;
  release(&np->lock);

  // 新增以下两行：父进程的 kpagetable 必须同步
  upg2ukpg(p->pagetable, p->kpagetable, 0, p->sz);
  sfence_vma();

  return pid;
```

#### 修改 5：`xv6/kernel/proc/proc.c` — growproc 添加 kpagetable 同步

```c
int growproc(int n)
{
  uint sz;
  struct proc *p = myproc();
  sz = p->sz;
  if(n > 0){
    if((sz = uvmalloc(p->pagetable, sz, sz + n)) == 0) {
      return -1;
    }
    upg2ukpg(p->pagetable, p->kpagetable, p->sz, sz);  // 新增
  } else if(n < 0){
    sz = uvmdealloc(p->pagetable, sz, sz + n);
  }
  p->sz = sz;
  sfence_vma();  // 新增
  return 0;
}
```

#### 修改 6：`xv6/kernel/proc/exec.c` — exec 中 kpagetable 更新后添加 sfence_vma

```c
  uvmunmap(p->kpagetable, 0, PGROUNDUP(old_sz) / PGSIZE, 0);
  upg2ukpg(p->pagetable, p->kpagetable, 0, p->sz);
  sfence_vma();  // 新增：刷新 TLB 后再释放旧页表

  proc_freepagetable(oldpagetable, old_sz);
```

### 结果

- 系统能正常启动到 shell
- `ls` 等命令可以正常执行
- 不再出现 `invalid instruction` panic
- 出现了双重 `$ $` 提示符的小问题（系空命令导致的额外 fork 循环，不影响功能）

---

## 关键技术要点总结

### K210 v1.9.1 与 v1.10+ 的差异

| 特性 | v1.10+ | K210 v1.9.1 |
|------|--------|-------------|
| 页表基址寄存器 | `satp` | `sptbr` (CSR 0x180) |
| 分页模式控制 | `satp.MODE` | `mstatus.VM[4:0]` |
| TLB 刷新指令 | `sfence.vma` | `sfence.vm` (0x10400073) |
| Load 缺页异常 | scause=13 | scause=5 |
| Store 缺页异常 | scause=15 | scause=7 |
| S 模式外部中断 | 支持 (SEIE) | 不支持，需 M 模式代理 |

### rustsbi 外部中断代理机制

```
M-mode MachineExternal 中断
  → mie::clear_mext()        // 关闭 M 态外部中断
  → stval = 9                // 标记为转发的外部中断
  → mip::set_ssoft()         // 触发 S 态软件中断
  → S-mode: scause=0x8000000000000001, stval=9
  → S-mode 调用 plic_claim() 获取真实 IRQ
  → 处理中断
  → S-mode 调用 sbi_set_mie() // 重新使能 M 态外部中断
```

### COW 与 TLB 一致性的关键原则

1. **任何 PTE 修改后必须调用 sfence_vma**：K210 v1.9.1 无硬件 TLB 一致性，必须显式刷新
2. **kpagetable 必须与用户页表保持同步**：`copyin_new`/`copyinstr_new` 在 K210 上直接使用虚拟地址通过 kpagetable 访问
3. **sfence_vma 后追加 fence.i**：确保指令缓存也被刷新，防止取指使用过期映射
4. **kfree 后尤其需要 TLB 刷新**：kfree 将释放的页面填充为 0x01（垃圾数据），若 TLB 仍缓存该物理页会导致非法指令

---

## 涉及的源代码文件

| 文件路径 | 修改内容 |
|---------|---------|
| `xv6/bootloader/rustsbi-k210/src/main.rs` | 使能 mext/msoft；修正 MachineExternal 处理流程 |
| `xv6/kernel/include/sbi.h` | 修正 SBI 扩展 ID (0x0A000004/0x0A000005) |
| `xv6/kernel/include/riscv.h` | sfence_vma 在 K210 上追加 fence.i |
| `xv6/kernel/trap/trap.c` | 添加 v1.9.1 缺页异常编码 (scause=5/7) |
| `xv6/kernel/vm/vm.c` | uvmcowmalloc/uvmcopy 添加 sfence_vma |
| `xv6/kernel/proc/proc.c` | fork 同步父进程 kpagetable；growproc 添加 upg2ukpg+sfence_vma |
| `xv6/kernel/proc/exec.c` | exec 的 kpagetable 更新后添加 sfence_vma |

---

## 问题四：echo/cat 等用户程序无法正常工作（exec 未传递 argc/argv）

### 现象

- `echo hi` 无输出
- `cat 1.txt` 卡住（阻塞在 stdin 读取）
- `echo hi > 1.txt` 可以正常写入文件
- `sh`（shell）本身可以正常工作

### 调试过程

1. **通过文件重定向测试**：`echo hi > 1.txt` 成功写入文件且内容正确，说明进程创建、ELF 加载、文件系统均正常。问题不在 I/O 路径。

2. **对比参考实现**：参考 `/home/xikao/xv6_k210/xv6-k210/kernel/exec.c`，发现关键差异——`exec()` 返回前必须设置 `p->trapframe->a0` 和 `p->trapframe->a1`。

3. **确认根因**：RISC-V 调用约定中，`a0` = 第一个参数，`a1` = 第二个参数。用户程序链接时使用 `-e main -Ttext 0`，以 `main` 为直接入口点（无 CRT 启动代码）。xv6 的 `syscall()` 将返回值写入 `p->trapframe->a0`：
   ```c
   p->trapframe->a0 = syscalls[num]();
   ```
   因此 `return argc` 即设置 `a0 = argc`。但 `a1`（argv 指针）必须显式设置。

   当前 `exec()` 的问题：
   - `return 0` 而非 `return argc`，`main(argc, argv)` 收到的 `argc` 总是 0
   - 未设置 `p->trapframe->a1`，`argv` 为垃圾值
   - 使用了带 3 槽位 fake header 的 ustack 布局（`ustack[3+MAXARG+1]`），而非标准 argv 数组

4. **为什么不同命令表现不同**：
   - `echo hi`：argc 为 0（或垃圾小值），for 循环不执行，无输出
   - `cat 1.txt`：argc 为垃圾（≤1），`cat(0)` 走 stdin 分支，阻塞等待键盘输入
   - `echo hi > 1.txt`：文件重定向替换 fd 1，写入与 argc 无关
   - `sh`：声明为 `main(void)`，不读取 a0/a1

### 修复

**文件：`kernel/proc/exec.c`**

#### 修改 1：ustack 声明

```c
// 修改前：
uint64 argc, sz, sp, ustack[3+MAXARG+1];

// 修改后：
uint64 argc, sz, sp, ustack[MAXARG+1], stackbase;
```

#### 修改 2：栈溢出边界

```c
uvmclear(pagetable, sz-2*PGSIZE);
sp = sz;
stackbase = sp - PGSIZE;  // 新增：栈溢出检测边界
```

#### 修改 3：参数压栈逻辑（完整重写）

```c
// 修改前（带 3 槽位 fake header 的错误布局）：
for(argc = 0; argv[argc]; argc++) {
    ...
    sp = (sp - (strlen(argv[argc]) + 1)) & ~(sizeof(uint64)-1);
    ustack[3+argc] = sp;
}
ustack[3+argc] = 0;
ustack[0] = 0xffffffff;  // fake return PC
ustack[1] = argc;
ustack[2] = sp - (argc+1)*sizeof(uint64);  // argv pointer
sp -= (3+argc+1) * sizeof(uint64);

// 修改后（标准 argv 数组布局，与参考一致）：
for(argc = 0; argv[argc]; argc++) {
    ...
    sp -= strlen(argv[argc]) + 1;
    sp -= sp % 16;           // RISC-V 16 字节对齐
    if(sp < stackbase) goto bad;
    ustack[argc] = sp;
}
ustack[argc] = 0;
sp -= (argc+1) * sizeof(uint64);
sp -= sp % 16;
if(sp < stackbase) goto bad;
```

#### 修改 4：设置 a1 并返回 argc

```c
// 新增：a1 指向用户栈上的 argv 数组
p->trapframe->a1 = sp;

// 修改前：
return 0;

// 修改后：
return argc;  // syscall() 将返回值写入 p->trapframe->a0
```

### 结果

- `echo hi` 正常输出 `hi`
- `cat 1.txt` 正常读取并显示文件内容
- `main(argc, argv)` 正确接收命令行参数
- QEMU 和 K210 双平台均正常工作

---

## 问题五：DMA 缓存一致性问题（K210 uncached alias）

### 现象

K210 平台上 SD 卡读取的数据有时不正确，表现为文件内容错乱或程序加载失败。

### 调试过程

1. **分析 K210 内存架构**：K210 的 8MB SRAM 有两个地址别名：
   - `0x80000000`：带 cache 的映射（CPU 默认使用）
   - `0x40000000`：不带 cache 的映射（uncached alias）

2. **DMA 写入 vs CPU 读取的不一致**：DMA 直接写入物理内存，绕过 CPU cache。若 DMA 写入的物理页在 cache 中已有缓存行，后续 CPU 读取可能命中旧的 cache 行而非 DMA 写入的新数据。

3. **定位问题文件**：`kernel/driver/spi.c` 中 SD 卡读写使用 `kalloc()` 分配的临时缓冲区，地址在 `0x80000000` 范围的 cached 区域。DMA 传输完成后，CPU 读取该缓冲区时可能命中旧的 cache 行。

### 修复

#### 修改 1：spi.c 使用 uncached alias

**文件：`kernel/driver/spi.c`**

在 DMA 传输前将临时缓冲区地址转换为 uncached alias，传输结束后恢复：

```c
#ifndef QEMU
// K210: Convert cached address to uncached alias for DMA
uint64 uncached_buf = (uint64)tmp_buf - 0x80000000 + 0x40000000;
dmac_set_single_mode(DMAC_CHANNEL1, ..., uncached_buf, ...);
#else
dmac_set_single_mode(DMAC_CHANNEL1, ..., tmp_buf, ...);
#endif
```

#### 修改 2：vm.c 添加 uncached 区域映射

**文件：`kernel/vm/vm.c`**

K210 uncached alias 区域 (`0x40000000`) 必须在页表中建立直接映射，否则访问该地址会触发 page fault：

```c
// kvminit() 中添加：
kvmmap(0x40000000, 0x40000000, 0x800000, PTE_R | PTE_W);

// ukvminit() 中添加：
ukvmmap(pagetable, 0x40000000, 0x40000000, 0x800000, PTE_R | PTE_W);
```

#### 修改 3：dmac.c 添加内存屏障

**文件：`kernel/driver/dmac.c`**

DMA 等待完成后添加 `__sync_synchronize()` 确保内存可见性：

```c
void dmac_wait_done(dmac_channel_number_t channel_num)
{
    dmac_wait_idle(channel_num);
    __sync_synchronize();  // 新增：确保 DMA 写入对 CPU 可见
}
```

### 结果

K210 平台上 SD 卡数据读取正确，文件系统操作稳定。

---

## 时钟配置（RustSBI 启动初始化）

### 背景

K210 的时钟树由 RustSBI **上电时初始化完成**，xv6 内核直接使用现成时钟，不主动配置 PLL。内核只是通过 sysctl 驱动读取实际频率，并为自己用到的外设配置分频阈值（I2C/SPI，见下）。

### 时钟树结构（芯片手册）

芯片只有一个外部 26MHz 晶振（IN0），经 3 个 PLL 倍频后分发给各外设：

- **PLL0**：CPU 及除 I2S 外所有外设的主时钟源
- **PLL1**：AI 工作时钟
- **PLL2**：I2S 工作时钟（可选输入 IN0 / PLL0 / PLL1）

PLL 公式（整数运算，[kernel/driver/sysctl.c](kernel/driver/sysctl.c) `sysctl_pll_get_freq`）：

```
F_OUT = F_IN / NR × NF / OD
```

外设时钟 = 时钟源 / ((threshold + 1) × 2)（偶数分频）或 / (2 << threshold)（ACLK 域）。

### 实际频率

| 时钟 | 频率 | 来源 |
|---|---|---|
| IN0（外部晶振） | 26MHz | `SYSCTRL_CLOCK_FREQ_IN0`（sysctl.c:19） |
| PLL0 | 780MHz | 反汇编实测 `ld` 精确读回 780MHz（见外设驱动调试文档） |
| CPU / ACLK（threshold=0） | 390MHz | PLL0 / (2<<0) = 780/2（实测：SPI 输入 = PLL0/2） |
| SPI0-2（threshold=0） | 390MHz | PLL0 / ((0+1)×2) = 780/2（sysctl.c:832-838） |
| I2C0-2（threshold=3） | 97.5MHz | PLL0 / ((3+1)×2) = 780/8（sysctl.c:844-846） |
| mtime（CLINT，0x02000000） | 15.6MHz，**实测 15.5MHz** | PLL0 / CLINT_CLOCK_DIV(50) = 780/50（SDK clint.h:64，非 sysctl TIMER） |

### mtime 时基与 tick 修正

- xv6 的 `r_time()` 读 `time` CSR（[riscv.h:45-50](kernel/include/riscv.h#L45-L50)），RustSBI 用 K210 **CLINT**（0x02000000）的 mtime/mtimecmp 提供标准 RISC-V 定时器，与 TIMER0 外设无关。
- **分频系数**：SDK `clint.h:64` 硬编码 `CLINT_CLOCK_DIV (50)`，`clint.c:60-64` 明确 `clint_timer_get_freq() = sysctl_clock_get_freq(SYSCTL_CLOCK_CPU) / CLINT_CLOCK_DIV`。
- **频率 = PLL0 / 50 = 780/50 = 15.6MHz**（推断：实测 15.5MHz × 50 ≈ 780MHz；若按 CPU 时钟 390MHz/50 仅得 7.8MHz，与实测不符 → CLINT 实际挂在 PLL0 时钟域，而非 ACLK）。
- 实测 15.5MHz 与 15.6 差 0.6%：PLL0 实际输出 vs 标称 780MHz 的硬件量化误差。
- 这是 tick 修正定稿 `INTERVAL=15600000/200` 的原因——按实测值而非标称值。此前假设 390MHz timebase → 5ms，实测却 125ms/tick（25 倍偏差），全部源于该时钟（详见 UART 烧录调试记录）。

> **勘误**：早期版本写"RustSBI 用 TIMER0 外设 + `timer_clk_sel=1` + `threshold_TIMER0=24` → 780/50"——这是**根据实测 15.5MHz 反推**的分频值，并非从寄存器/文档读到，且反推对象错误。手册 `aclk_divider_sel` 只支持 2/4/8/16 四档（threshold 0-3），不存在 threshold=24；TIMER0 是独立外设（0x502D0000），与 mtime 无关。正确结论以本节为准。

### 分频系数是谁设置的

CLINT 的分频是**硬件固定/SDK 常量**（`CLINT_CLOCK_DIV=50`），不是 RustSBI 或内核写入的寄存器阈值：

- 内核 grep：`sysctl_clock_set_clock_select(TIMER*)`、`sysctl_clock_set_threshold(THRESHOLD_TIMER*)`、`sysctl_clock_enable(SYSCTL_CLOCK_TIMER0)` 调用数**全部为 0**。
- 内核只配置自己用到的外设：I2C `threshold=3`（i2c.c:73）、SPI `threshold=0`（spi.c:66）、各外设时钟使能（uart.c:166 / dmac.c:274 / spi.c:64 / i2c.c:68）。
- **含义**：mtime 频率带硬件量化误差，软件侧只能实测校准 `INTERVAL`，无法改频率。若要提频 CPU，需在 RustSBI 侧配置 PLL0（当前 780MHz，未提频），属独立大工作。

### 涉及文件

| 文件 | 内容 |
|---|---|
| `kernel/driver/sysctl.c` | PLL/时钟频率计算：`sysctl_pll_get_freq` / `sysctl_clock_get_freq` |
| `kernel/include/sysctl.h` | PLL0/1/2 寄存器结构、时钟/阈值/时钟源枚举 |
| `kernel/include/param.h` | `INTERVAL`（tick 间隔），K210 下 `15600000/200` |
| `kernel/driver/uart.c` | 数据口 UART1 时钟使能（uart.c:166） |
| `kernel/driver/i2c.c` / `spi.c` | 外设分频 threshold 配置（驱动自配） |
| `reference/kendryte-standalone-sdk/lib/drivers/clint.c` / `include/clint.h` | mtime 分频依据：`CLINT_CLOCK_DIV=50`、`clint_timer_get_freq()` |
| `reference/kendryte-standalone-sdk/lib/bsp/include/platform.h` | `CLINT_BASE_ADDR=0x02000000` |

---

## 问题六：终端出现多余 $ 提示符和空行（consoleintr 回车重复处理）

### 现象

在 shell 中执行命令后，输出中出现多余的 `$` 提示符和空行：

```
$ echo hi

hi
$ $ echo hi > 1.txt

$ $ cat 1.txt

hi
$ $
```

### 调试过程

1. **追踪数据流**：用户输入 → UART 收到字符 → `uartintr()` → `consoleintr()` → 写入 `cons.buf` → `consoleread()` → `gets()` → `getcmd()` → shell 处理。

2. **关键发现**：K210 的 SiFive UART 在按下回车时发送两个字符——`\r`（CR，0x0D）和 `\n`（LF，0x0A）。QEMU 仿真的 16550a UART 仅发送 `\r`。

3. **定位代码**：`consoleintr()` 第 165 行无条件将 `\r` 转换为 `\n`：
   ```c
   c = (c == '\r') ? '\n' : c;
   ```
   这导致 `\r` 和 `\n` 都被转换为 `\n`，在输入缓冲区中产生两个连续的换行符。

4. **传播链分析**：
   - 第一个 `\n` 终止用户的实际命令 → shell 正常执行
   - 第二个 `\n` 成为一条空命令 → `gets()` 立即返回 `"\n"` → `getcmd()` 返回 0 → shell `fork()`/`wait()` 后打印 `$ ` 提示符
   - `consputc()` 对两个 `\n` 各输出一次，导致空行

5. **对比参考实现**：`/home/xikao/xv6_k210/xv6-k210/kernel/console.c` 使用编译时条件编译：
   ```c
   #ifndef QEMU
   if (c == '\r') break;     // K210: 丢弃 \r
   #else
   c = (c == '\r') ? '\n' : c;  // QEMU: 转换 \r 为 \n
   #endif
   ```

### 修复

**文件：`kernel/devsw/console.c`**

```c
// 修改前：
c = (c == '\r') ? '\n' : c;

// 修改后：
#ifndef QEMU
      if(c == '\r') break;     // on k210, "enter" will input \n and \r
#else
      c = (c == '\r') ? '\n' : c;
#endif
```

- K210 平台（未定义 `QEMU`）：丢弃 `\r`，只保留 `\n`
- QEMU 平台（定义了 `QEMU`）：将单独的 `\r` 转换为 `\n`

### 结果

终端不再出现多余的 `$` 提示符和空行，命令输出整洁。

---

## 涉及的源代码文件（本次新增）

| 文件路径 | 修改内容 |
|---------|---------|
| `kernel/proc/exec.c` | 重写参数压栈逻辑：移除 fake header，设置 `a1=sp`（argv），`return argc`（a0） |
| `kernel/devsw/console.c` | consoleintr 添加 `#ifndef QEMU` 守卫，K210 丢弃 `\r`，QEMU 转换 `\r`→`\n` |
| `kernel/driver/spi.c` | DMA 传输前将临时缓冲区地址转换为 K210 uncached alias (0x40000000) |
| `kernel/driver/dmac.c` | dmac_wait_done 添加 `__sync_synchronize()` 内存屏障 |
| `kernel/vm/vm.c` | kvminit/ukvminit 添加 0x40000000 uncached 区域的直接映射 |
